import { ArrowRight, ChefHat, Clock3, Sparkles } from 'lucide-react'
import { Link } from 'react-router'
import { Button } from '../components/ui/button'
import { recipes } from '../data/mockData'

export function RecipesPage() {
  return <div className="w-full max-w-5xl space-y-8"><div><p className="flex items-center gap-2 text-sm font-bold uppercase tracking-[0.14em] text-[#426a5a]"><ChefHat size={16} /> From your kitchen</p><h1 className="mt-2 text-4xl font-black tracking-tight">Recipes worth rescuing for</h1><p className="mt-3 max-w-xl text-stone-600">Recipes are ranked by how much of your food they use, so the right meal is easier to spot.</p></div><div className="grid gap-5 lg:grid-cols-3">{recipes.map((recipe) => <article key={recipe.id} className="flex flex-col rounded-[2rem] border border-[#e5e1d5] bg-white p-5 shadow-[0_8px_24px_rgba(70,67,52,0.05)]"><div className="flex items-center justify-between"><span className="inline-flex items-center gap-1 rounded-full bg-[#dce9de] px-2.5 py-1 text-xs font-black text-[#426a5a]"><Sparkles size={13} /> {recipe.match}% match</span><span className="flex items-center gap-1 text-xs font-bold text-stone-500"><Clock3 size={14} /> {recipe.time} min</span></div><h2 className="mt-7 text-2xl font-black">{recipe.title}</h2><p className="mt-3 text-sm leading-6 text-stone-500">{recipe.description}</p><p className="mt-5 text-xs font-bold uppercase tracking-[0.12em] text-[#b84f49]">{recipe.rescue}</p><div className="mt-5 flex flex-1 flex-wrap content-start gap-2">{recipe.ingredients.map((ingredient) => <span key={ingredient} className="rounded-full bg-[#f6f1e5] px-3 py-1.5 text-xs font-semibold text-stone-600">{ingredient}</span>)}</div><Button className="mt-7 w-full bg-[#426a5a] text-white hover:bg-[#355747]"><Link to="/consumption" className="flex w-full items-center justify-center gap-2">Cook this <ArrowRight size={16} /></Link></Button></article>)}</div></div>
}
