import { ArrowLeft, Check, CircleCheck, History, Minus, Plus } from 'lucide-react'
import { Link } from 'react-router'
import { Button } from '../components/ui/button'
import { foodItems } from '../data/mockData'

export function ConsumptionPage() {
  return <div className="w-full max-w-3xl space-y-8"><Link to="/" className="inline-flex items-center gap-2 text-sm font-bold text-[#426a5a]"><ArrowLeft size={16} /> Back to kitchen</Link><div><p className="flex items-center gap-2 text-sm font-bold uppercase tracking-[0.14em] text-[#426a5a]"><History size={16} /> Consumption</p><h1 className="mt-2 text-4xl font-black tracking-tight">How much did you use?</h1><p className="mt-3 text-stone-600">Update quantities as you cook and your kitchen will stay honest.</p></div><section className="space-y-3">{foodItems.slice(0, 3).map((item) => <div key={item.id} className="flex items-center gap-4 rounded-3xl border border-[#e5e1d5] bg-white p-4"><div className="flex size-12 items-center justify-center rounded-2xl" style={{ backgroundColor: item.accent }}><CircleCheck size={21} /></div><div className="min-w-0 flex-1"><h2 className="font-bold">{item.name}</h2><p className="text-sm text-stone-500">Available: {item.quantity} {item.unit}</p></div><div className="flex items-center gap-2"><Button size="icon" variant="outline" aria-label={`Use less ${item.name}`}><Minus size={16} /></Button><span className="w-8 text-center font-bold">0</span><Button size="icon" variant="outline" aria-label={`Use more ${item.name}`}><Plus size={16} /></Button></div></div>)}</section><div className="rounded-3xl bg-[#f6f1e5] p-5"><p className="font-bold">Leftovers?</p><p className="mt-1 text-sm leading-6 text-stone-600">Save remaining portions as a new item with a fresh opened date and a shorter timer.</p><label className="mt-4 flex items-center gap-3 text-sm font-semibold"><input type="checkbox" className="size-5 accent-[#426a5a]" /> Save leftovers to inventory</label></div><Button className="h-12 w-full bg-[#426a5a] text-base text-white hover:bg-[#355747]"><Check size={18} /> Confirm consumption</Button></div>
}
