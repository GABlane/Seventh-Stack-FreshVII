import { Leaf, LockKeyhole, Mail, UserRound } from 'lucide-react'
import { cloneElement, isValidElement, useState } from 'react'
import { Link, useNavigate } from 'react-router'
import { Button } from '../components/ui/button'
import { login, register } from '../firebase/services/auth.service'

type AuthMode = 'login' | 'signup'

export function AuthPage() {
  const [mode, setMode] = useState<AuthMode>('login'); const [displayName, setDisplayName] = useState(''); const [email, setEmail] = useState(''); const [password, setPassword] = useState(''); const [error, setError] = useState(''); const [submitting, setSubmitting] = useState(false)
  const navigate = useNavigate(); const signup = mode === 'signup'
  async function submit(event: React.FormEvent<HTMLFormElement>) { event.preventDefault(); setError(''); setSubmitting(true); try { signup ? await register(email, password, displayName) : await login(email, password); navigate('/app') } catch { setError(signup ? 'We could not create your account. Check your details and try again.' : 'We could not sign you in. Check your email and password.') } finally { setSubmitting(false) } }
  return <main className="flex min-h-svh items-center justify-center bg-[#eaf8fa] px-5 py-8"><div className="grid w-full max-w-6xl overflow-hidden rounded-[2rem] border border-[#c6dde5] bg-white shadow-[0_18px_50px_rgba(66,106,90,0.12)] lg:grid-cols-[0.8fr_1.2fr]"><section style={{ background: 'linear-gradient(145deg, #193b5a, #193b5a 55%, #193b5a)' }} className="relative hidden overflow-hidden p-10 text-white lg:flex lg:min-h-[620px] lg:flex-col lg:justify-between"><div className="absolute -bottom-24 -left-16 size-72 rounded-full border-[32px] border-white/15" /><div className="relative"><Link to="/" className="text-sm font-black tracking-[0.18em] text-[#ffe167]">FRESHVII</Link><Leaf className="mt-20 text-[#ffe167]" size={42} /><h1 className="mt-6 max-w-sm text-5xl font-black leading-[1.05] text-white">Make room for good food.</h1><p className="mt-5 max-w-sm leading-7 text-white/90">Your personal kitchen companion for fresher choices, less waste, and easier meals.</p></div><p className="relative text-sm text-white/85">Track it. Rescue it. Enjoy it.</p></section><section className="p-6 sm:p-10 lg:p-14"><div className="mb-8 lg:hidden"><Link to="/" className="text-sm font-black tracking-[0.18em] text-[#193b5a]">FRESHVII</Link></div><div className="mb-8"><p className="text-sm font-bold uppercase tracking-[0.14em] text-[#193b5a]">Your kitchen, in sync</p><h2 className="mt-2 text-3xl font-black text-[#193b5a]">{signup ? 'Create your account' : 'Welcome back'}</h2><p className="mt-3 text-[#6f8b95]">{signup ? 'Start keeping good food in rotation.' : 'Pick up where your kitchen left off.'}</p></div><div className="mb-7 grid grid-cols-2 rounded-2xl bg-[#d9eef3] p-1"><Tab active={!signup} onClick={() => { setMode('login'); setError('') }}>Sign in</Tab><Tab active={signup} onClick={() => { setMode('signup'); setError('') }}>Create account</Tab></div><form onSubmit={submit} className="space-y-5">{signup && <Field label="Your name" icon={<UserRound size={20} />}><input required value={displayName} onChange={(event) => setDisplayName(event.target.value)} placeholder="Alex Morgan" /></Field>}<Field label="Email address" icon={<Mail size={20} />}><input required type="email" value={email} onChange={(event) => setEmail(event.target.value)} placeholder="you@example.com" /></Field><Field label="Password" icon={<LockKeyhole size={20} />}><input required minLength={6} type="password" value={password} onChange={(event) => setPassword(event.target.value)} placeholder="At least 6 characters" /></Field>{error && <p role="alert" className="rounded-xl bg-[#ffe3e3] px-4 py-3 text-sm text-[#d94444]">{error}</p>}<Button disabled={submitting} style={{ minHeight: '3.75rem' }} className="w-full bg-[#193b5a] text-base text-white hover:bg-[#193b5a]">{submitting ? 'Working...' : signup ? 'Create my account' : 'Sign in to FRESHVII'}</Button></form><p className="mt-7 text-center text-xs text-[#6f8b95]">By continuing, you agree to keep your food data private and yours.</p></section></div></main>
}
function Tab({ active, onClick, children }: { active: boolean; onClick: () => void; children: React.ReactNode }) { return <button type="button" onClick={onClick} className={`rounded-xl px-4 py-3 text-sm font-bold ${active ? 'bg-white text-[#193b5a] shadow-sm' : 'text-[#5b7086]'}`}>{children}</button> }
function Field({ label, icon, children }: { label: string; icon: React.ReactNode; children: React.ReactNode }) { const input = isValidElement<{ className?: string }>(children) ? cloneElement(children, { className: 'h-16 min-w-0 flex-1 bg-transparent text-base text-[#193b5a] outline-none placeholder:text-[#9a9a85]' }) : children; return <label className="block"><span className="mb-2 block text-sm font-bold text-[#193b5a]">{label}</span><span style={{ minHeight: '4rem' }} className="flex w-full items-center gap-3 rounded-xl border border-[#c6dde5] bg-[#f6fcfd] px-5 text-[#6f8b95] focus-within:border-[#193b5a]">{icon}{input}</span></label> }
