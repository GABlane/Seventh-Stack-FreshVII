import { ChefHat, Clock3, Sparkles } from 'lucide-react'
import { Link } from 'react-router'
import { Button } from '../ui/button'
type Recipe = { id: string; title: string; description: string; time: number; match: number; ingredients: string[]; rescue: string; rescuedIngredients?: string[] }
export function RecipeCard({ recipe, compact = false }: { recipe: Recipe; compact?: boolean }) { const rescued = recipe.rescuedIngredients ?? []; return <article className={`flex ${compact ? 'h-[18rem]' : 'h-[36rem]'} flex-col overflow-hidden rounded-[1.5rem] border border-[#e5e1d5] bg-white shadow-[0_8px_24px_rgba(70,67,52,0.05)]`}><div className={`relative flex ${compact ? 'h-20' : 'h-36'} shrink-0 items-center justify-center bg-[#dce9de] text-[#426a5a]`}><ChefHat size={compact ? 42 : 70} strokeWidth={1.3} /><span className="absolute left-4 top-4 inline-flex items-center gap-1 rounded-full bg-white/90 px-2.5 py-1 text-xs font-black text-[#426a5a]"><Sparkles size={13} /> {recipe.match}% match</span><span className="absolute right-4 top-4 rounded-full bg-white/90 px-2.5 py-1 text-xs font-bold text-[#7d806e]"><Clock3 size={13} className="mr-1 inline" /> {recipe.time} min</span></div><div className={`flex min-h-0 flex-1 flex-col ${compact ? 'p-3' : 'p-5'}`}><h2 className={`${compact ? 'text-base' : 'text-xl'} font-black text-[#2d493d]`}>{recipe.title}</h2><p className={`${compact ? 'mt-1 line-clamp-2 text-xs' : 'mt-2 text-sm'} text-[#7d806e]`}>{recipe.description}</p><p className={`${compact ? 'mt-2' : 'mt-4'} text-xs font-bold uppercase tracking-[0.12em] text-[#9a6d20]`}>{recipe.rescue}</p><div className={`${compact ? 'mt-2 gap-1' : 'mt-4 gap-2'} flex flex-1 flex-wrap content-start`}>{recipe.ingredients.map((ingredient) => <span key={ingredient} className={`rounded-full ${compact ? 'px-2 py-1 text-[10px]' : 'px-3 py-1.5 text-xs'} font-semibold ${rescued.includes(ingredient) ? 'bg-[#dce9de] text-[#426a5a]' : 'bg-[#f6f1e5] text-[#7d806e]'}`}>{ingredient}</span>)}</div><div className={`${compact ? 'mt-2' : 'mt-5'} grid grid-cols-2 gap-2`}><Button asChild className="h-10 bg-[#426a5a] text-xs text-white hover:bg-[#355747]"><Link to={`/app/consumption?recipe=${encodeURIComponent(recipe.title)}`}><ChefHat size={14} /> Cook This</Link></Button><Button asChild variant="outline" className="h-10 border-[#d8d1c0] bg-[#fdfbf6] text-xs text-[#2d493d]"><Link to={`/app/recipes/${recipe.id}`}>View recipe</Link></Button></div></div></article> }
