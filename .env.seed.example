# Credentials for the demo/seed Firebase account.
# Copy this file to .env.seed and fill in the values.
# .env.seed is gitignored — never commit it.
DEMO_EMAIL=demo@freshvii.app
DEMO_PASSWORD=your_demo_password

# npx tsx scripts/seed/run-all.ts --reset